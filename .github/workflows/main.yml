on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 185.25.206.75
        username: multicraft
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: ftps
        port: 22
        server-dir: provola/
        timeout: 60000
        dry-run: true
